# =============================================================================
# EMP CLOUD — Environment Configuration
# Copy this file to .env and fill in the values.
# =============================================================================

# ---- Server ----
NODE_ENV=development
PORT=3000
BASE_URL=http://localhost:3000

# ---- Database (EmpCloud master) ----
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=secret
DB_NAME=empcloud
DB_AUTO_MIGRATE=true

# ---- Redis ----
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ---- OAuth2 / JWT (RS256) ----
# Generate keys with: pnpm generate-keys
RSA_PRIVATE_KEY_PATH=./keys/private.pem
RSA_PUBLIC_KEY_PATH=./keys/public.pem
ACCESS_TOKEN_EXPIRY=15m
REFRESH_TOKEN_EXPIRY=7d
AUTH_CODE_EXPIRY=10m
ID_TOKEN_EXPIRY=1h

# ---- CORS ----
# Comma-separated list of allowed origins (sub-module URLs)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176

# ---- Email ----
# Two transports are supported. The service tries them in order:
#   1) SendGrid — set SENDGRID_API_KEY for production.
#   2) SMTP — set SMTP_HOST (and creds if your relay needs them) for
#      local dev with Mailpit/MailHog or self-hosted relays.
# When neither is set, sendEmail() is a no-op and just logs the
# would-be delivery (dev still works without configuring anything).
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=noreply@empcloud.com
SENDGRID_FROM_NAME=EMP Cloud
# SMTP fallback — defaults below match Mailpit (https://github.com/axllent/mailpit)
# running locally on port 1025 with no auth.
SMTP_HOST=
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@empcloud.com
# Public URL of the client app — used to build password-reset and
# invitation links. Falls back to BASE_URL if not set.
APP_URL=http://localhost:5173

# ---- Rate Limiting ----
RATE_LIMIT_AUTH_MAX=20
RATE_LIMIT_AUTH_WINDOW_MS=900000
RATE_LIMIT_API_MAX=100
RATE_LIMIT_API_WINDOW_MS=60000

# ---- Billing Integration ----
# Base URL of the EMP Billing module API
BILLING_MODULE_URL=http://localhost:4001
PAYROLL_MODULE_URL=http://localhost:4000
MONITOR_MODULE_URL=http://localhost:5000
INTERNAL_SERVICE_SECRET=
OPENAI_API_KEY=
ASSISTANT_MODEL=
ASSISTANT_OPENAI_API_KEY=
# Direct Gemini proxy key. GLB_KEY is also accepted as a fallback.
ASSISTANT_GEMINI_API_KEY=
GLB_KEY=
# Optional OpenAI-compatible Chat Completions endpoint (for example a gateway or local provider).
ASSISTANT_OPENAI_BASE_URL=
ASSISTANT_OPENAI_ORGANIZATION=
ASSISTANT_OPENAI_PROJECT=
# Set true only for compatible providers that require the legacy max_tokens field.
ASSISTANT_USE_LEGACY_MAX_TOKENS=false
ASSISTANT_MAX_TOKENS=2048
ASSISTANT_MAX_TOOL_ROUNDS=8
ASSISTANT_MODULE_MAX_RETRIES=2
ASSISTANT_MODULE_RETRY_BASE_MS=300
ASSISTANT_PROVIDER_MAX_RETRIES=2
ASSISTANT_PROVIDER_RETRY_BASE_MS=500
BILLING_API_KEY=

# ---- Logging ----
LOG_LEVEL=debug
LOG_FORMAT=pretty

# ---- Legacy emp-monitor compatibility ----
# Shared secret the EMP Field app sends as `secretKey` in the body of every
# /api/v3/hrms/* and /api/v3/user/fieldAllEmployeeList request. Must match the
# value emp-monitor used (its FIELD_TRACKING_SECRET_KEY) so the existing field
# client works unchanged. Leave blank to keep the field surface disabled
# (fail-closed: every request is rejected when this is empty).
FIELD_TRACKING_SECRET_KEY=
