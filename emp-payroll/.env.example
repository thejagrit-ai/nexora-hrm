# ============================================================================
# EMP-PAYROLL ENVIRONMENT CONFIGURATION
# Copy this to .env and fill in your values
# ============================================================================

NODE_ENV=development
PORT=4000
HOST=0.0.0.0

# ---------------------------------------------------------------------------
# EmpCloud Master Database (users, organizations, auth)
# Shared across all EMP modules. Authentication happens here.
# ---------------------------------------------------------------------------
EMPCLOUD_DB_HOST=localhost
EMPCLOUD_DB_PORT=3306
EMPCLOUD_DB_USER=root
EMPCLOUD_DB_PASSWORD=
EMPCLOUD_DB_NAME=empcloud

# ---------------------------------------------------------------------------
# Payroll Module Database — payroll-specific tables only
# Options: mysql | postgres | mongodb
# ---------------------------------------------------------------------------
DB_PROVIDER=mysql

# SQL (MySQL / PostgreSQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=emp_payroll
DB_POOL_MIN=2
DB_POOL_MAX=10

# MongoDB (when DB_PROVIDER=mongodb)
MONGO_URI=mongodb://localhost:27017/emp_payroll

# ---------------------------------------------------------------------------
# Redis (queues, caching, rate limiting)
# ---------------------------------------------------------------------------
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ---------------------------------------------------------------------------
# JWT Authentication
# ---------------------------------------------------------------------------
JWT_SECRET=
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# ---------------------------------------------------------------------------
# Tenant secrets master key (AES-256-GCM)
# ---------------------------------------------------------------------------
# Used to encrypt per-org credentials at rest (RazorpayX key secret + webhook
# secret today; future tenant-supplied API keys). 32 bytes, hex-encoded.
# Generate with:  openssl rand -hex 32
# IMPORTANT: rotating this key invalidates every previously-encrypted secret —
# the Settings page will surface a clear error and the operator must re-enter
# each tenant's Razorpay credentials.
PAYROLL_SECRETS_KEY=

# ---------------------------------------------------------------------------
# Email (payslip delivery, notifications)
# Provider preference: SendGrid when SENDGRID_API_KEY is set, else SMTP.
# Set SENDGRID_API_KEY for prod; SMTP_* are kept as a fallback for local dev.
# ---------------------------------------------------------------------------
SENDGRID_API_KEY=
EMAIL_FROM_EMAIL=payroll@empcloud.com
EMAIL_FROM_NAME=EMP Payroll

# Legacy SMTP fallback — only used when SENDGRID_API_KEY is empty.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=payroll@empcloud.com

# ---------------------------------------------------------------------------
# CORS
# ---------------------------------------------------------------------------
CORS_ORIGIN=http://localhost:5173

# ---------------------------------------------------------------------------
# Payroll Configuration
# ---------------------------------------------------------------------------
PAYROLL_COUNTRY=IN
PAY_FREQUENCY=monthly

# ---------------------------------------------------------------------------
# Cloud HRMS Integration
# When enabled, payroll computation fetches attendance/leave data from
# EMP Cloud's HRMS APIs instead of the local payroll database.
# ---------------------------------------------------------------------------
EMPCLOUD_API_URL=http://localhost:3000/api/v1
USE_CLOUD_HRMS=false
